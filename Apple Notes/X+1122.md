![[Pasted Graphic 5.png]]


X+1122
http://localhost:37799/webroot/decision/login/initialization


![[Pasted Graphic 1 2.png]]


从你执行 `ps -ef | grep mysqld` 的输出结果来看：

进程启动参数里的 `--basedir=/usr/local/mysql` 已经明确告诉你 —— **MySQL 的安装根目录是 `/usr/local/mysql`**。

后续若要查看 MySQL 的配置、数据目录等，也可以从这行参数里进一步提取（比如 `--datadir=/usr/local/mysql/data` 是数据存储目录，`--log-error=...` 是错误日志路径等）。
X+1126. Root 3306

```
/usr/local/mysql/bin/mysql --version
```

```
/usr/local/mysql/bin/mysql -u root -p'Xjny+1126' -h 127.0.0.1 -P 3306
```

```
[client] user = root password = Xjny+1126 host = 127.0.0.1 port = 3306
```

#配置 #mysql