# 标签数据底座3.0 · 迭代日志（自动更新版）

> 本文档作为迭代日志，记录每一轮对话的 **共识 / 需求精华 / 产出物 / 下一步**。用于新对话的启动包导入，保持上下文连续。

---

## 🟢 当前目标

930 前上线 P0=13 个标签，搭建可扩展的 **标签数据底座3.0**，长期支持 200+ 标签扩展。

---

## ✅ 已达成共识

- **标签层级**：三级结构（一级/二级/三级）；三级为最小颗粒。
- **分类方法**：
  - **A 类**：线下上翻（多源融合+证据阶梯）。
  - **B 类**：纯线上契约逻辑（100% 可重算）。
- **质量闸门**：Acc≥90%、Cov≥90%、Fresh=T+1、Stable≤5%。
- **产出字段统一**：七件套 `value/source/conf/ver/class/evidence_state/trace`。
- **主体维度**：签约方 > POP > Store + 基本工商/位置信息。

---

## 📦 已交付产物

1. **批次1｜治理10表 DDL**【已完成】【见文档】[243]
2. **批次2｜宽表 DDL**（初版全宽，后转 Hot 层）【见文档】[241]
3. **批次3｜P0=13 标签规格卡**【见文档】[245]
4. **批次4｜规则 DSL & SQL 样例**【见文档】[244]
5. **批次5｜情报源模板 & 入库脚本**【见文档】
6. **补丁｜Tall 真相层 + Hot 宽层**【见文档】[242]

---

## 🔄 架构升级共识

- **真相层（Tall）**：`tag_value_fact` （一行=站点×日期×tag\_id，多槽位）。
- **证据表**：`tag_trace`（trace 外置）。
- **热点白名单**：`tag_wide_hot_list`（P0=13；后可扩展30\~50）。
- **Hot 宽层**：`tag_wide_daily`（仅展开白名单标签）。
- **兼容视图**：`tag_wide_daily_compat`（为旧消费方保留）。

---

## 📝 启动 Prompt 模板

```
我正在推进滴滴加油的商户标签建设，目标是930前上线P0=13个标签，搭建可扩展的标签数据底座3.0。
已确认的共识：
- 标签3层级：一级/二级/三级
- 标签分类：A类(线下上翻+证据阶梯)/B类(纯线上契约)
- 质量闸门：Acc≥90%、Cov≥90%、Fresh=T+1、Stable≤5%
- 已产出：治理10表DDL、宽表DDL(Hot层)、P0规格卡、DSL&SQL样例、情报源模板
- 架构升级：Tall真相层(tag_value_fact)+Trace(tag_trace)+热点白名单(tag_wide_hot_list)+Hot宽层(tag_wide_daily)+兼容视图
新对话目标：在此基础上继续设计XXX（写具体任务）
```

---

## 🔜 下一步议题（待展开）

-

---

## 📌 使用说明

- 每一轮对话结束 → 更新本日志（补充新共识/产物/下一步）。
- 新对话 → 复制“启动 Prompt 模板”开场，保持上下文不丢失。
- 本文档即作为“共识黑匣子”，自动补全迭代历史。

