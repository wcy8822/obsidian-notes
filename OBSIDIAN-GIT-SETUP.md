# Obsidian Git 插件配置指南

> 自动备份 Obsidian 笔记到 GitHub

## ✅ 已完成的配置

- ✅ GitHub 仓库已创建：https://github.com/wcy8822/obsidian-notes
- ✅ Git 仓库已初始化
- ✅ 首次提交已推送（538 个文件）
- ✅ 插件配置文件已生成
- ✅ 敏感信息已排除

---

## 🔧 Obsidian Git 插件配置

### 步骤 1: 打开插件设置

1. 打开 Obsidian
2. 按 `Cmd + ,` 打开设置
3. 左侧菜单点击 **"社区插件"**
4. 找到 **"Git"** 插件
5. 点击 **"选项"** 按钮

### 步骤 2: 配置参数

在插件设置页面，按照以下参数配置：

#### 基础设置

```
自动备份间隔: 5 分钟
├─ 自动提交: ✅ 开启
├─ 自动推送: ✅ 开启
├─ 自动拉取: ✅ 开启
└─ 启动时拉取: ✅ 开启

提交消息格式: backup: {{date}}
日期格式: YYYY-MM-DD HH:mm:ss

推送前拉取: ✅ 开启
同步方式: merge
```

#### 高级设置

```
显示状态栏: ✅ 开启
显示变更文件: ❌ 关闭
推送后重载插件: ❌ 关闭
```

### 步骤 3: 验证配置

配置完成后，在 Obsidian 中：

1. 按 `Cmd + P` 打开命令面板
2. 输入 `Git: Create backup`
3. 回车执行
4. 查看右下角状态栏，应该显示 "Git: 备份成功"

---

## 📋 完整配置文件（JSON格式）

如果设置界面太复杂，可以直接编辑配置文件：

**文件位置：**
```
/Users/ixu/Documents/obsidian/.obsidian/plugins/obsidian-git/data.json
```

**完整配置（已自动生成）：**
```json
{
  "autoBackupInterval": 5,
  "autoCommit": true,
  "autoPush": true,
  "autoPull": true,
  "pullOnStartup": true,
  "pullBeforePush": true,
  "disablePush": false,
  "commitMessage": "backup: {{date}}",
  "commitDateFormat": "YYYY-MM-DD HH:mm:ss",
  "customMessageOnAutoBackup": false,
  "autoBackupAfterFileChange": false,
  "treeStructure": false,
  "diffCommand": "git diff",
  "diffContextLines": 3,
  "reloadPluginAfterPush": false,
  "syncMethod": "merge",
  "listChangedFilesInMessageBody": false,
  "showStatusBar": true
}
```

---

## 🎯 工作原理

```
你编辑笔记
    ↓
每 5 分钟自动触发
    ↓
1. Git 提交（本地）
    ↓
2. Git 拉取（从 GitHub 获取最新）
    ↓
3. 合并变更
    ↓
4. Git 推送（上传到 GitHub）
    ↓
✅ 备份完成
```

---

## 💡 日常使用

### 自动备份（无需操作）

插件会每 5 分钟自动备份，你什么都不用做。

### 手动备份（可选）

如果你刚编辑完重要内容，想立即备份：

**方法 1: 命令面板**
```
Cmd + P → 输入 "Git: Create backup" → 回车
```

**方法 2: 快捷键（可自定义）**
```
设置 → 快捷键 → 搜索 "Git" → 创建快捷键
```

### 查看备份状态

**在 Obsidian 中：**
- 右下角状态栏显示 Git 状态
- 绿色点：已同步
- 红色点：有待推送的更改

**在终端中：**
```bash
cd /Users/ixu/Documents/obsidian
git log --oneline -5
```

**在 GitHub 中：**
访问：https://github.com/wcy8822/obsidian-notes/commits/main

---

## 🔙 版本回滚

### 查看历史

```bash
cd /Users/ixu/Documents/obsidian
git log --oneline -10
```

### 恢复单个文件

```bash
# 查看文件历史
git log --oneline -- README.md

# 恢复到指定版本
git checkout <commit-hash> -- README.md
```

### 回滚整个仓库

```bash
# 回滚到指定版本
git reset --hard <commit-hash>

# 强制推送（会覆盖远程）
git push --force
```

---

## ⚠️ 注意事项

### 1. 已排除的文件

以下文件不会被备份（在 .gitignore 中）：

```gitignore
# Obsidian 插件和缓存
.obsidian/plugins/
.obsidian/workspace
.obsidian/workspace-mobile
.obsidian/app.json
.obsidian/live-sync/

# 敏感信息文件
Clippings/ChatGPT-API-信息.md
Inbox/AI/API.md

# macOS 系统文件
.DS_Store
.AppleDouble
.LSOverride

# 临时文件
*.tmp
*.bak
*~
*.base
未命名*

# 日志文件
livesync_log_*.md
```

### 2. 如果遇到冲突

**场景：** 在多台设备同时编辑同一文件

**解决方法：**
```bash
cd /Users/ixu/Documents/obsidian

# 查看冲突
git status

# 编辑冲突文件，解决后
git add .
git commit -m "fix: 解决合并冲突"
git push
```

### 3. 备份频率调整

如果你想修改备份频率：

**在 Obsidian 插件设置中：**
- 修改 "自动备份间隔"（单位：分钟）

**或者在配置文件中：**
```json
{
  "autoBackupInterval": 10  // 改为 10 分钟
}
```

---

## 📊 备份统计

**首次备份统计：**
- 文件数：538 个
- 仓库大小：43MB
- 备份时间：2026-01-16

**查看仓库大小：**
```bash
cd /Users/ixu/Documents/obsidian
du -sh .git
```

---

## 🔍 故障排查

### 问题 1: 插件无法启动

**解决：**
1. 确认已安装 Git 插件
2. 检查 `.obsidian/plugins/obsidian-git/` 目录存在
3. 重启 Obsidian

### 问题 2: 自动备份不工作

**解决：**
1. 检查是否在 Obsidian 设置中启用了 "自动提交" 和 "自动推送"
2. 查看 Obsidian 控制台（`Cmd + Option + I`）是否有错误
3. 手动测试：`Cmd + P` → `Git: Create backup`

### 问题 3: 推送失败

**解决：**
```bash
cd /Users/ixu/Documents/obsidian

# 测试连接
ssh -T git@github.com

# 手动推送
git push
```

---

## 📚 相关资源

- **GitHub 仓库**: https://github.com/wcy8822/obsidian-notes
- **Obsidian Git 插件文档**: https://github.com/Vincent0314/obsidian-git
- **Git 官方文档**: https://git-scm.com/doc

---

## ✅ 配置检查清单

完成以下检查，确保配置正确：

- [ ] Obsidian Git 插件已安装
- [ ] 插件已启用
- [ ] 自动提交：开启
- [ ] 自动推送：开启
- [ ] 自动拉取：开启
- [ ] 备份间隔：5 分钟
- [ ] 首次手动备份成功
- [ ] GitHub 仓库可以看到提交

---

**配置完成时间**: 2026-01-16

**备份状态**: ✅ 自动运行中

**下次备份**: 5 分钟内
